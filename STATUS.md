# 到期提醒助手原型状态

> 最后更新：2026-05-03

## 当前阶段

1. 阶段：M2 本地事项闭环
2. 状态：已接入 SQLite 本地仓储，新增事项、首页读取、全部页读取、已处理、延后动作已形成最小闭环

## 当前目标

1. 完成 Android-first 本地到期事项管理
2. 跑通本地通知闭环
3. 保持年轻化、轻盈、现代的界面风格
4. 下一阶段接入 Android 本地通知权限、通知调度与取消

## 当前注意

1. 本原型目录由 `create-expo-app` 初始化，目录内存在独立 `.git`
2. 上级 `AI-Practice-Lab` 当前不是 git 仓库
3. 本阶段先不执行提交操作
4. 首版暂用 `IconGlyph` 文字图标组件，后续 UI 稳定后再替换正式图标库
5. M2 日期输入暂用 `YYYY-MM-DD` 文本框，后续需要替换为更现代的日期选择交互
6. M2 只做本地 SQLite 数据闭环，尚未接入通知、广告、会员支付和云同步
